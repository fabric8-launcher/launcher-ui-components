{
  "backend_url": "${LAUNCHER_BACKEND_URL}",
  "keycloak_url": "${LAUNCHER_KEYCLOAK_URL}",
  "keycloak_realm": "${LAUNCHER_KEYCLOAK_REALM}",
  "keycloak_client_id": "${LAUNCHER_KEYCLOAK_CLIENT_ID}",
  "segment_tracker_token": "${LAUNCHER_TRACKER_SEGMENT_TOKEN}"
}